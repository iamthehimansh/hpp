import std/{fmt, io, time};

def int bit[32];
def long bit[64];

fn main() -> int {
    print_int(1);
    print_newline();

    sleep_ms(100);

    print_int(2);
    print_newline();

    sleep_ms(100);

    print_int(3);
    print_newline();

    return 0;
}
